#include <stdio.h>
#include <stdlib.h>
#include "utils.h"

int main(int argc, char *argv[])
{
    uint32_t row = 10;
    uint32_t column = 10;
    double val = 0; 

    Matrix *m = init_matrix(row, column);

    return EXIT_SUCCESS;
}